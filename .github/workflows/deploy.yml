name: Build Docker Images

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and push client image
        uses: docker/build-push-action@v5
        with:
          context: ./client
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/votex-client:latest
            ${{ secrets.DOCKER_USERNAME }}/votex-client:${{ github.run_number }}
      
      - name: Build and push server image
        uses: docker/build-push-action@v5
        with:
          context: ./server
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/votex-server:latest
            ${{ secrets.DOCKER_USERNAME }}/votex-server:${{ github.run_number }}
      
      - name: Summary
        run: |
          echo "✅ Images built and pushed successfully!"
          echo "📦 Client: ${{ secrets.DOCKER_USERNAME }}/votex-client:${{ github.run_number }}"
          echo "📦 Server: ${{ secrets.DOCKER_USERNAME }}/votex-server:${{ github.run_number }}"

          # run 2